const { Command } = require("reconlx");

module.exports = new Command({
  // options
  description: ``,
  userPermissions: [],
  category: "",
  run: async ({ client, interaction, args }) => {
    // Code
  },
});
